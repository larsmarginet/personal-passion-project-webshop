export default {
    currentEvent(state) {
        return state.currentEvent;
    },
    currentSong(state) {
        return state.currentSong;
    },
    bubble(state) {
        return state.bubble;
    },
    roomId(state) {
        return state.roomId;
    },
    setError(state) {
        return state.error;
    }
}